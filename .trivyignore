# 此檔案用於忽略已確認且無法修復或不影響安全性的漏洞
# 格式：CVE-ID (每行一個編號)

# CVE-2026-25646: libpng 堆緩衝區溢位（libpng 1.6.54-r0 → 修復版 1.6.55-r0）
# 適用：frontend 映像（georgjung/nginx-brotli:1.29.5-alpine, Alpine 3.23.3）
# 原因：升級 libpng 需 apk upgrade，會導致預編譯 Brotli 模組 ABI 不相容、nginx 無法啟動
# 風險：LOW — 前端僅提供靜態資源，不解析或處理使用者上傳的 PNG
# 最後檢查：2026-02-28 — 映像已升級至 1.29.5-alpine，但 libpng 仍為 1.6.54-r0，CVE 尚未修復
# 下次檢查：2026-Q2 或上游釋出新 tag 時
CVE-2026-25646

# CVE-2026-28390: libssl3 OpenSSL DoS via NULL pointer dereference
# 適用：backend 映像（gcr.io/distroless/cc-debian12）
# 原因：distroless base image 自帶 libssl3，無法透過 apt upgrade 修復；
#       覆蓋 .so 會造成 ABI 不相容導致應用程式無法啟動
# 風險：LOW — DoS 需特製 TLS 連線，API 位於反向代理後方
# 下次檢查：distroless image 更新後移除
CVE-2026-28390
