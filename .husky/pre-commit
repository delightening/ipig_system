#!/bin/sh
# 個資掃描（阻擋含疑似個資的 commit，見 scripts/pii-scan.mjs）
node scripts/pii-scan.mjs --staged || exit 1

# 前端 lint-staged（從專案根目錄執行）
npx lint-staged
# 後端 cargo fmt check（如已安裝）
(cd backend && cargo fmt --check 2>/dev/null) || true
