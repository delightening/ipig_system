#!/bin/sh
# 個資掃描（commit message）。
#
# ⚠️ 這一關是 2026-08-25 才補上的：在此之前 commit message 從來沒有被掃過，
# 一則列出 6 位同仁姓名與各自負責計畫份數的訊息連續三次 push 全數通過。
# pre-commit 掃的是 staged 內容，那時訊息還沒寫；只有 commit-msg 拿得到它。
node scripts/pii-scan.mjs --commit-msg "$1" || exit 1
