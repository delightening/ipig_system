# Clippy 設定檔
# 文件：https://doc.rust-lang.org/clippy/configuration.html

# 允許較長的函式（ERP 系統的 handler 與 service 函式通常較大）
too-many-lines-threshold = 200

# 允許較多函式參數
too-many-arguments-threshold = 10

# 認知複雜度閾值
cognitive-complexity-threshold = 30

# enum variant 大小門檻（clippy::large_enum_variant）
# 預設 200 bytes 太寬：enum 的 size 等於最大 variant 的 size，
# 而 error / event enum 會出現在每個 Result<T, E> 與每個 async future 裡，
# 成本乘上併發數。降到 64 讓「一個胖 variant 拖累全部」變成看得見的 warning。
enum-variant-size-threshold = 64
