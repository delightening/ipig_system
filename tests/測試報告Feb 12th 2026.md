# 修復整合測試失敗 — 完成報告

## 修復的 Bug

| # | 模組 | 根因 | 修復 |
|---|------|------|------|
| 1 | AUP 後端 | `&req.content[..47]` 在中文 UTF-8 字元中間切割導致 panic | 改用 `chars().take(47)` 安全截斷 |
| 2 | Animal 測試 | 測試送 `"deceased"` 但 `PigStatus` 只有 3 個值 | 改為 `"completed"` |
| 3 | AUP 測試 | `get_status()` 從頂層讀 `status` 但 API 回傳嵌套在 `protocol.status` | 修正 JSON 路徑 |

## 修改的檔案

- [protocol.rs](file:///c:/System%20Coding/ipig_system/backend/src/services/protocol.rs#L1549) — UTF-8 安全截斷
- [test_animal_full.py](file:///c:/System%20Coding/ipig_system/tests/test_animal_full.py#L361) — status 值修正
- [test_aup_full.py](file:///c:/System%20Coding/ipig_system/tests/test_aup_full.py#L49) — JSON 路徑修正

## 測試結果

```
✅  AUP     — 14/14 通過
✅  ERP     — 9/9 通過
✅  Animal  — 21/21 通過
⏱ 總耗時: 5.3 秒
```
